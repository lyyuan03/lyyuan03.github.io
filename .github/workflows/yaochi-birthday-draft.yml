name: Retired Yaochi Draft Sync
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Existing event bodies are managed by Firestore admin and Secure Jinmu Event Series. No public static body sync is permitted."
