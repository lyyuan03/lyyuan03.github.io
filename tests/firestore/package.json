{
  "name": "lyyuan-firestore-security-tests",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.2"
  },
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --only firestore --project demo-membership-expiry --config firebase.expiry-tests.json 'node --test tests/firestore/expiry.test.mjs'"
  }
}
